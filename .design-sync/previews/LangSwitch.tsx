import { LangSwitch } from 'ai-researcher';

/** Три сегмента в ряд — начертание для шапки страницы. */
export const Segments = () => <LangSwitch />;

/** Одна компактная кнопка — для узких мест вроде мобильной панели. */
export const Cycle = () => <LangSwitch variant="cycle" />;
