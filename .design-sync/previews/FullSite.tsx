import { FullSite } from 'ai-researcher';

/**
 * Весь сайт AI Исследователя одной страницей — дерево баз + таблица MindSheet +
 * панели. Страница сама тянет данные через шим @/lib/api (реальные строки
 * каталога), поэтому пропсы не нужны. Правится как единый экран.
 */
export const Default = () => <FullSite />;
