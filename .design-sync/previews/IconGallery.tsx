import { IconGallery } from 'ai-researcher';

/** Весь набор в рабочем кегле — так иконки стоят в тулбарах и меню. */
export const Default = () => <IconGallery />;

/** Крупный кегль: видно толщину обводки и как иконка держит форму. */
export const Large = () => <IconGallery size={28} />;
