---
category: Navigation
---
# SavedViews

Сохранённые виды таблицы: текущая сортировка, уровни группировки, фильтры и
строка поиска складываются под именем и восстанавливаются одним кликом.
Хранится локально, по ключу `ais:views:<base>`.
