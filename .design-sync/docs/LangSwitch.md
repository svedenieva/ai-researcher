---
category: Foundation
---
# LangSwitch

Переключатель языка (uk / ru / en) в двух начертаниях: `segments` — три
сегмента в ряд, `cycle` — одна компактная кнопка, перебирающая языки. Язык
берётся из `LangProvider` и хранится в `localStorage`.
