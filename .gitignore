node_modules/
.next/
.env*
!.env*.example
*.tsbuildinfo
*.log

.vercel

# Subagent-driven-development scratch workspace
.superpowers/
playground/dist

# Claude Design bridge — local-only state (watermark of last executed pull)
.design-sync/.cache/

# Claude Code — local/personal settings (may hold MCP tokens); never commit
.claude/settings.local.json
